/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://minuchoi.com',
  generateRobotsTxt: true,
  // Additional options can be added here if needed
};

module.exports = config;
